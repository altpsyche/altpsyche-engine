# Journal

**This file is the register a person reads after a run nobody watched.** It is not a queue —
[ROADMAP.md](ROADMAP.md) is the only thing that queues work, and every row below names the item
that tracks it. A row needing work nobody is tracking needs a roadmap item too, in the same
commit.

**Why it exists here.** [ROADMAP.md](ROADMAP.md) says design decisions about this library are
recorded in the consuming repository's log, and that log is not reachable from this tree. An
unattended session that takes a call and writes it nowhere has taken an untracked, unreversible
call — which is the failure this file exists to prevent. So a call taken **in** this repository is
written **here**, and rows that also need an entry on the consuming side are marked `carry` so
somebody moves them across. That is a handover, not a decision changing home.

## What earns a row

Every one of these, whether or not anything went wrong:

- **A call taken with nobody watching.** What was decided, **how to reverse it**, and what would
  change the answer. An entry with no reversal recipe has not been written.
- **Something that could not be verified on this machine** — the card gate, a phone, a deployed
  address, a reproduction nobody has managed.
- **A risk accepted, a bar widened, or a number nobody has looked at.** Per RoadToPureEngine.md
  §17's amendment to decision 4: a number a gate accepts because its bar was widened is a number
  nobody has looked at.
- **A gate that passed for a reason that does not prove what the step claimed** — a picture that
  cannot move by construction, a count the recorder does not take.
- **Something a green gate is blind to**, changed anyway.
- **Uncertainty proceeded through.** Say what would settle it, not only that it is open.

Say what would settle it. A row that records a doubt and not its remedy is a doubt nobody can
close.

## Columns

| column | holds |
| --- | --- |
| date | when the row was written |
| item | the ROADMAP.md item this belongs to |
| kind | `call`, `unverified`, `risk`, `blind-gate`, `doubt` |
| carry | `carry` where the consuming repository's log also needs an entry, otherwise blank |
| what | the row itself, including how to reverse a call and what would settle a doubt |

---

## Rows

| date | item | kind | carry | what |
| --- | --- | --- | --- | --- |
| 2026-08-24 | 2 | call | | The program cache key now serialises `resources`, `pipelines`, `passes`, `present` and `swap` with `JSON.stringify`, joined to id and module text. Item 2 offered "or a key the caller supplies outright"; I took the computed-key arm and did **not** add a caller-supplied key field, because that would put a new field on `ShaderFrame` and move the export surface, and item 2 says the commit contains this and nothing else. **Reverse:** restore `renderer/index.ts`'s `key()` to `[shader.id, ...modules].join('\0')` and delete the `renderer-index.test.ts` case "keeps a program per resource set". **What would change the answer:** if a later item needs a caller to override the key (item 12's structural pipeline cache may), add the field then; JSON key order is builder-consistent here, but a resource built with keys in a different order would false-*miss* (harmless recompile), never false-*hit* — if that stops holding, switch to a canonical stable serialisation. |
| 2026-08-24 | 2 | blind-gate | | The fix is proven only against the fake WebGPU device: two frames equal in id and module text but with different uniform-block layouts now compile two pipelines instead of colliding on one. jsdom returns no WebGL 2 context, so the WebGL 2 backend's `createProgram` never runs this key path in the node suite — the browser gates hold that. The node suite also cannot show the *rendered* consequence (wrong buffer under a shared program); it shows only that the two frames no longer share a cached program. What would settle the rendered claim is a browser gate drawing both frames and reading back distinct pixels. |
| 2026-08-24 | 2 | risk | | The key serialises `resources` with `JSON.stringify`, and a generated resource carries its bytes as a `Uint8Array`, which stringifies to `{"0":1,"1":2,...}` rather than to anything compact. Measured on a 432,000-byte buffer, roughly a thousand cubes of interleaved f32: **4,640,891 key characters, 10.7x the byte count, 41.7 ms to build**. Correct, and retained in the `WeakMap` for the frame object's lifetime. It costs nothing today because every frame in the corpus is a fullscreen shader with tiny or no generated bytes; it lands at item 30, a thousand instanced objects, and item 36, an asset loaded at run time. **Not fixed here** because item 12's structurally-keyed pipeline cache explicitly deletes this narrower fix, so the cheap answer is to hash bytes rather than stringify them *there* rather than to grow this key now. **What would change the answer:** an item before 12 that puts real geometry through this path — if item 30 arrives first, hash the bytes here instead. Recorded by the reviewer of the run, not by the step: the step's own rows cover key ordering and the blind gate, and this cost went unrecorded. |
| 2026-08-24 | 3 | call | carry | Removed the exported `DocumentAddress` type and renamed the exported `documentAddresses(description)` to `documentNames(description)`, returning the documents' `name` strings instead of their `address` roles. The item mandated the union's removal; the rename and the choice to make `documentNames` the loader's fetch-and-key handle are mine, because a loader that keyed fetched text by `address` cannot carry two WGSL documents (both `wgsl`) and a function still called `documentAddresses` returning names would lie. `assembleFrame` now keys `texts` by `document.name` rather than by `texts.get(document.address)`. **Reverse:** restore `DocumentAddress = 'fragment'|'vertex'|'wgsl'` and the `address` field on `DocumentSpec` in `renderer/types.ts`; put `address` back on the three `documents:` literals (`renderer/frame.ts` ×2, `fixtures/shader-describe.ts` ×1); rename `documentNames` back and key it and `assembleFrame` on `address`; restore the `frameOf` override guard to `document.address === 'wgsl'` and the two `shader-describe.test.ts` assertions to `{ name, address }`. **What would change the answer:** if a rung ever needs two documents pointing at one physical file distinguished only by role, a name is no longer enough and the address would have to return — nothing in the corpus does this today. |
| 2026-08-24 | 3 | blind-gate | carry | The real loader that fetches document text over the network lives in the **consuming repository**, not here; this repo's `documentNames`/`assembleFrame` are the shape it must conform to. The node suite exercises the re-keying and draws the two-document frame against the fake WebGPU device (`tests/frame-documents.test.ts`), proving both texts arrive intact and a module compiles from each. What no gate here can see is that the consuming repo's fetch layer now keys by document **name** rather than **address** — that seam is exercised only when that repo is updated to match. **Carry:** the consuming loader must fetch and key by `documentNames(...)` and stop resolving an `address`. |
| 2026-08-24 | 5 | call | | The path gate is a vitest test, `tests/docs-paths.test.ts`, because `npm test` is the always-run gate suite and item 5 says the check "runs in the gate suite". It reads two shapes — Markdown link targets and backtick spans ending in a source extension — after stripping fenced code blocks, and resolves each against the working tree (by relative path or bare basename). A backtick span counts as a path only when it has a `/` **or** a lowercase-initial basename, so a `Type.field` read like `ShaderSource.glsl` is not mistaken for a file; the cost is that an uppercase-basename file cited in backticks (none exists today) would slip the backtick check, though a link to it still would not. **Reverse:** delete `tests/docs-paths.test.ts`. **What would change the answer:** if a real uppercase-named source file gets cited in backticks and later moves, tighten `looksLikePath` to consult the basename set rather than the case of the first letter. |
| 2026-08-24 | 5 | call | | The gate carries an allowlist, `ALLOWED_ABSENT`, of five paths that do not resolve on purpose: `docs/DEVICES.md`, `docs/TESTING.md` and `host/loop.ts` are files later roadmap items create, and `components/ui/WgslRefusal.tsx` and `public/shaders/build/manifest.json` are website paths [RoadToPureEngine.md](RoadToPureEngine.md) §3 row 12 names precisely to say they are gone. Without this, "finds no missing file" over all of `docs/` is unsatisfiable while the roadmap names future files and the debt table names dead ones. A third test asserts each entry is still absent, so an allowlist row a real file grows under is flagged rather than left masking a fresh stale path. This mirrors decision 4's widened-list discipline: a reasoned, asserted, reviewed exemption rather than a silent skip. **Reverse:** delete the allowlist and its test; the five paths then fail the gate. **What would change the answer:** when item 9 creates `docs/DEVICES.md` or item 39 creates `host/loop.ts`, the third test fails and that row is removed. |
| 2026-08-24 | 5 | blind-gate | | The gate checks only backtick-quoted and link-quoted paths, which is item 5's exact wording. `ABSTRACTION.md`'s Mermaid diagram names website paths (the source shaders, the content shaders, the site's artefact adapter, the surface hook and more) in node labels, and those sit inside a fenced block the gate strips, so they are neither checked nor changed — left as a labelled picture of the superseded website pipeline. So "loses every path that does not exist here" holds for the checkable surface (backtick and link spans, all now resolving or allowlisted) and not for diagram labels or bare prose. The cheap gates also prove only that paths resolve, not that the reworded prose is *true* — e.g. that "the site's surface hook" or "the site's `backends` gate" names a thing that exists over there. **What would settle it:** a reader of the consuming repository confirming those site-side names, or a later pass redrawing the diagram against §7's folders. |
